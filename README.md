Getting Started

Clone the repository or create a new project using this template.

Then install dependencies:

npm install
npm install --save-dev jest
npm install --save-dev @babel/preset-env

Start the development server:

npm run dev

This will start a local server and open the project in your browse
